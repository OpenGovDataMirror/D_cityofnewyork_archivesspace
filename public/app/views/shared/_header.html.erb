<section  id="header">
  <div class="row">
    <div class="col-sm-9">
      <h1>
        <% unless request.original_fullpath == '/' %>
          <a title="<%=t('brand.title_link_text') %>" href="<%= AppConfig[:public_proxy_url] %>">
        <% end %>
        <strong><%= t('brand.title') %></strong>
        <% unless request.original_fullpath == '/' %>
          </a>
        <% end %>
      </h1>
    </div>
    <div class="col-sm-3 hidden-xs"><img class="logo" src="<%= asset_path(AppConfig[:pui_branding_img]) %>" alt="NYC Department of Records Logo" /></div>
  </div>
</section>
